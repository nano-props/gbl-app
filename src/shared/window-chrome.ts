// Shared chrome height for renderer topbars, Win/Linux titleBarOverlay, and
// macOS traffic-light centering. Keep window chrome and renderer layout on the
// same constant so future tweaks do not drift across process boundaries.
export const WINDOW_TOPBAR_HEIGHT_PX = 40
